import { SectionList } from 'react-native';

import Reanimated     from "react-native-reanimated";
import LinearGradient from 'react-native-linear-gradient';


export const REASectionList    = Reanimated.createAnimatedComponent(SectionList);
export const REALinearGradient = Reanimated.createAnimatedComponent  (LinearGradient);
